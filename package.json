{
  "name": "my-github-mcp-server",
  "version": "1.0.0",
  "description": "A GitHub MCP (Model Context Protocol) server implementation",
  "type": "module",
  "main": "build/index.js",
  "bin": {
    "github-mcp-server": "./build/index.js"
  },
  "scripts": {
    "build": "tsc && chmod 755 build/index.js",
    "start": "node build/index.js",
    "dev": "npm run build && npm start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "files": ["build"],
  "keywords": ["mcp", "github", "model-context-protocol"],
  "author": "amotarao",
  "license": "ISC",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.18.1",
    "zod": "3"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "typescript": "^5.9.2"
  }
}
