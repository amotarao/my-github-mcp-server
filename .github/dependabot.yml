version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:00"
      timezone: "Asia/Tokyo"
    groups:
      react:
        patterns:
          - "react"
          - "react-dom"
          - "@types/react"
          - "@types/react-dom"
      next:
        patterns:
          - "next"
          - "eslint-config-next"
    open-pull-requests-limit: 5
    reviewers:
      - "amotarao"
    commit-message:
      prefix: "deps"
      include: "scope"
    ignore:
      - dependency-name: "zod"
        update-types: ["version-update:semver-major"]
